<% if (authenticated) { %>
	<button type='button' class='btn btn-primary signout ' value=''>
		 <i class="glyphicon glyphicon-log-out"></i> Sign out
	</button>
<% } else { %> 
	<button type='button' class='btn btn-primary login ' value=''>
		 <i class="glyphicon glyphicon-log-in"></i> Log in
	</button>
<% } %>

